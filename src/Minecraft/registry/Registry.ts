import {Block} from './Blocks/Block';

/**
 * Sets out the blocks in the registry.
 * @type {Map<string, Block>}
 * @kind constant
 * @see {@link Blocks}
 *
 * @todo items & entities;
 */
const Blocks: Map<string, Block> = new Map();
// items
// entities

export {Blocks};